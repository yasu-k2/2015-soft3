#!/usr/bin/env prolog

% check with
% prolog -f ./like.swi -t 'like(X,Y).'

like(taro,coffee).
like(hanako,tea).
like(taro,cocoa).

fly(X) :- airplane(X).
fly(X) :- superman(X).
airplane(jet_plane).
airplane(helicopter).
superman(taro).